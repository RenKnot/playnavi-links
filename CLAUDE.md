<!-- dev-bridge:managed:start (rendered by cli/bridge_repo_docs.py — この区間は手で編集しない) -->
# CLAUDE.md — playnavi-links 作業規律（profile `playnavi`）

`AGENTS.md` を最上位の入口とし、本ファイルは全サービス共通の作業規律を定めます。
`.bridge-tasks/orchestrator-rules.md` の禁止事項は本ファイルより上位です。
managed 区間より下の「リポ固有」節に、このリポだけの規律・索引が続きます。

## A. 事実の扱い（最重要: 捏造を構造的に防ぐ）

1. **コード・設定・スキーマ・API 挙動を述べる前に、必ず該当ファイルを read / grep するか、コマンドを実行して裏を取る。**
   記憶や推測で答えない。「既存コードはこう動くはず」で進めず、まず該当箇所を開く（検索 → 思考の順）。
2. **裏付けが取れない事実は「未確認」と明示する。** 憶測で空欄を埋めない。PR 番号・commit SHA・バージョン・
   ファイルパス・行番号・テスト結果などの具体値は、記憶からではなく**直前のコマンド出力から**取る。
3. **事実主張には根拠（`ファイルパス:行` またはコマンド出力）を併記する。** 根拠を出せない＝その主張は怪しい、
   というサイン。報告・要約・PR 説明・Slack 投稿のいずれでも、断定する事実には出所を付ける。
4. **「やった」と報告する前に、やった結果を実コマンドで確認してから書く。** commit SHA は `git-commit` の返却 JSON、
   push は `git-push` の返却 JSON、PR は `pr-create` の返却値、型/テストは実際に走らせた出力を根拠にする。
   「通ったはず」「作成済みのはず」と書かない。
5. **権限・エラーの診断にも同じ規律を適用する。** 「権限が無い / 壊れている」と断定する前に、実際にその操作を試した
   tool の生エラーと対象絶対パスを取得する。resume・再起動・uuid クリア等を原因や対処として挙げない（そういう仕組みは無い）。

> これらは「だいたい正しく、間違えても根拠併記で自分でバレる」ための規律。思考が浅いターンでの捏造を、出所要求で表面化させる。

## B. スコープ規律

- 依頼された範囲を超える仕様変更を勝手に入れない。必要だと思ったら先に Slack で確認する。
- 既存の関数 signature を変える前に、呼び出し側を grep で全部洗う。
- 既存機能を壊さないアドオン方式で変更する。

## C. 安全境界

- `.env` を読まない、作らない、コミットしない。接続値・鍵・トークンをドキュメントやチャットへ転記しない。
- 本番 DB 書込、本番 deploy、protected branch への push は行わない。Git の mutation は Bridge 親経由のみ。
- 破壊的なクラウド操作（DROP / DELETE / TRUNCATE / WHERE なし UPDATE）は、実行前に read-only SELECT で影響件数を確認し、
  最終回答に残す。`CLOUD_APPROVAL_REQUIRED` で止まったら request_id を添えて operator に承認を求め、承認後は**同一内容**を再実行する。
- 他 profile の state / secrets / ssh / worktree には触れない。denied は仕様であり、迂回しない。

## D. Bridge 非対話運用で繰り返した失敗の根治ルール（全サービス共通）

1. **1 トリガー 1 ターン。** 完了・質問・エラーは必ず最終回答（Bridge 親が Slack へ投稿）で閉じ、stdout で終わらない。
   「このまま進めます」等の継続を匂わす発言は禁止。「完了」はコード経路だけでなく実機 / 実データで要件充足を確認してから言う。
2. **要件は task JSON の `current_event.raw_text` が唯一の正。** git status の未追跡や docs/ の他タスク成果物から推測しない。
   operator の回答・承認・選択を絶対に捏造しない（無ければ「未回答」と明記）。
3. **operator がエラーログ / traceback を貼ったら「実際に落ちた」。** reassure せず、末尾の例外型と `file:line` から根因を特定して
   コードを直す。エラー文言の握り潰しで逃げない。
4. **「ストップ / 止まれ / 動くな」が来たら、キュー中の全作業（git / サーバ / メモリ / サマリ）を即破棄し、現状をいじらず
   最小 1 行で受領のみ返す。**
5. **同じ TASK branch に積む前に、その PR がまだ OPEN かを Bridge control または `pr-create` の返却値で確認する。**
   MERGED 済みブランチへの commit / push は main に入らない。判明したら新規 PR を上げ直す。「push 段階で初めて気づいて
   cherry-pick で後始末」を起こさない。
6. **Supabase の bulk / chunk 処理は read / write とも 100 件（重い RPC は 50 件）から始め、`57014` statement timeout が出た
   範囲だけを半分に分割して再試行する。** 1 件でも timeout、または非 timeout エラーなら即停止。同じ大きさの反射的再試行、
   初回 100 件超、`statement_timeout=0` は禁止。
7. **レビュー / 承認を求める投稿は対象を一意に特定して渡す。** PR は生 URL（複数なら全 URL）、画面はその ID、DB は確認 SQL。
   「レビューして」だけで URL 無しは禁止。別タスクの内容を混ぜない。
8. **検証は原則 agent が実行し、operator に複数回の手作業を要求しない。** 権限・実機・本番安全上、人間の実行が必要な場合も、
   安全な one-shot スクリプトまたは単一コマンドに集約し、操作は原則 1 回、結果の判定と追加調査は agent が担う。
   SQL 1 ファイルを 1 回実行すれば完結する作業に、runner・ラッパ・その専用テストを追加しない。
9. **operator に DB の手動 SQL 実行を依頼する時は、実行対象ごとに役割・実行要否・順序と、レビュー対象 commit に固定した
   GitHub ファイル URL を明示する。** PR URL やファイル名、`forward → verify → audit` のような略記だけで渡さない。
   rollback は通常実行しないと明記する。
10. **Slack の複数行本文に文字列としての `\n` を書かない。** 本文には実改行を入れ、送信直前にリテラル `\n` が残っていないか検査する。
    Slack mrkdwn は `*bold*`（`**bold**` は禁止）、PR URL は装飾なしの生 URL。
11. **PR のレビュー / マージ依頼は PR URL で終えず、同じ投稿にマージ後からタスク完結までの全工程を先回りして書く。**
    各工程の担当者・順序・実行対象・開始 / 成功 / 停止条件・必要な返信・次ターンの処理を明示し、「マージ後に手順を出す」と後回しにしない。
12. **DB 関数 / スキーマの契約を変える時は、SQL 適用と呼び出し側（バッチ / アプリ）の配備を 1 セットとして扱い、
    SQL 適用 → 呼び出し側配備の順で行う。** 片側だけ先に本番へ出さない。既存の判定条件を無効化する仕様変更は、
    その条件を参照している関数を `pg_get_functiondef` で全部洗ってから配備する。
13. **本番 runner を持つサービスでは、operator に 40 桁 commit SHA や `EXPECTED_SHA` 系環境変数を取得・転記・手入力させない。**
    runner 自身が `origin/main` を更新後、`main` かつ clean かつ `HEAD == origin/main` を書込み前に検証し、実行 SHA を
    `git rev-parse HEAD` から内部確定する。固定 SHA はレビュー済み artifact へ内包し、人の差し替え値にしない。
14. **新規実装・再実装・adhoc・runner・外部 API / DB 接続を作る前に、同じリポの現行・到達可能・成功証跡付きの類似経路を
    `rg` / `git log -S` で検索し、要件ごとに「踏襲元 `file:line` / 成功証跡 / そのまま再利用 / 意図的差分 / 差分の検証」を
    対応表にする。** 外部契約は前例の有無にかかわらず現行公式仕様とも照合する。探索せず独自実装へ進むこと、
    既存経路との差を説明・検証しないことは禁止。安全な契約が確定しない（認証・DB・deploy）場合は実装前に停止して確認する。
15. **Slack の各ターンは直近コメントへの一問一答で閉じず、スレッド全体の未完了な元目的を最後まで引き取る。**
    新事実で従来案が成立しなくなったら、誤りの訂正だけで止めず、影響する判断・破棄する手順・代替経路・担当者・
    直後の操作・成功 / 停止条件を同じ投稿で差し替える。agent が調査・判断できる次手を人へ押し返さない。
16. **人向け説明は、会話全体から「今、相手が答えてほしい一点」を 1 文で固定し、その対象へ直接答える。**
    「何が起きるか」を聞かれたら、通常時 → 混雑時 → 失敗時 → 現時点の確度の順で利用者に見える結果を説明し、
    試験方法・実装案・別論点へすり替えない。「どう確認するか」を聞かれた時だけ機材・操作・試験方法を説明する。
17. **人向けの文章では、相手が使っていない専門用語・業界語・英語略語・内部名を説明なしで使わない。**
    最初に、誰が何を操作し、成功時に何が起き、失敗・途中停止時に何が表示されるかを平易な日本語で書く。
    技術名が確認に必要な場合だけ初出で日本語の意味を併記し、以後はその日本語名を使う。
18. **Slack で文脈のない質問をしない。** 質問前に、何をなぜ今決めるのか、各選択肢で作業・時間・リスク・最終状態がどう変わるか、
    確認済み事実と未確認事項、根拠付き推奨を本文だけで理解できる形で示す。read-only 調査と工学判断で決められる事項は
    質問せず決定して報告する。質問が必要な場合は識別子 a / b / c、推奨は必ず `a`、回答パス 3 通り
    （✅ 全推奨 / `Q3: b` 差分 / `a a b a` 並び）を末尾に付ける。設計ドキュメントへの誘導で済ませない。
19. **レビューゲートは operator の明示レスポンスのみで進む。** タイマーや「N 分後に推奨で進めます」等の自動進行は禁止。

## E. 最終回答の様式

人向けの最終回答には「誰が次に動くか / 直後の操作 / 成功条件・停止条件 / operator に必要な返信」を書く。
完了で操作不要なら「operator の追加操作なし / タスク完結」と明記する。引き継ぎタスクでは、目を通した情報・ファイルの一覧を
証拠として、専門用語は定義ツリーを先に作って論理名で統一し、いずれもスマホで読める形で報告する。
<!-- dev-bridge:managed:end -->

## リポ固有の規律・索引（この節は人間が管理する。Bridge は上書きしない）

- 正典ドキュメント・機能 ID 台帳・ディレクトリ構成・コマンド早見表をここに置く
- このリポで特に注意すべき契約（例: identity 列の生成契約、bulk 処理の上限）があればここに追記する
